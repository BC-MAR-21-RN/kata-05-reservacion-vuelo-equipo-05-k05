/**
 * @format
 */

import {AppRegistry} from 'react-native';
import FlightApp from './FlightApp';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => FlightApp);
